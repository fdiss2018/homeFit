# Logs
logs
*.log
npm-debug.log*
firebase-debug.log*
firebase-debug.*.log*

# Firebase cache
.firebase/

# Firebase config — clés de projet, à ne pas committer (voir firebase-config.example.js)
public/firebase-config.js

# URL du backend (diffère entre dev local et prod) — voir api-config.example.js
public/api-config.js

# Claude Code local settings
.claude/

# Uncomment this if you'd like others to create their own Firebase project.
# .firebaserc

# Coverage
coverage
.coverage

# Dependencies
node_modules/

# Divers
.env
*.tgz

# Backend — secrets (clé de compte de service Firebase, clé Gemini, bearer statique)
# voir backend/.env.example
backend/.env
