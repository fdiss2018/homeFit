{
  "name": "homefit",
  "type": "module",
  "scripts": {
    "test":       "vitest",
    "test:run":   "vitest run",
    "test:cover": "vitest run --coverage"
  },
  "devDependencies": {
    "vitest": "^2.0.0"
  }
}
